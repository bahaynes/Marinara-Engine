#!/bin/sh
# Authoritative, full, from-scratch check — same command CI runs. Slower on
# purpose: this is the last gate before code leaves your machine.
# Bypass with `git push --no-verify` when you deliberately need to.
cd "$(git rev-parse --show-toplevel)" && pnpm check
